#include "main.h"
/**
 * _abs - computes the absolute value of an integer.
 * @int the interger whose absolute value is sort for
 *Return: the absolute value
*/ 

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
		return (-n);
}	
